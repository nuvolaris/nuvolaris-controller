apiVersion: v1
kind: Service
metadata:
  labels:
    app: rook-static
  name: rook-static-svc
  namespace: nuvolaris
spec:
  ports:
  - name: http
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: rook-static
  sessionAffinity: None
  type: ClusterIP
